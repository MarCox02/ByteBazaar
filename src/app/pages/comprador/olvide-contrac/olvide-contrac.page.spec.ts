import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OlvideContracPage } from './olvide-contrac.page';

describe('OlvideContracPage', () => {
  let component: OlvideContracPage;
  let fixture: ComponentFixture<OlvideContracPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OlvideContracPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
